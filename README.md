# 📦 Instalação das Dependências do Projeto

Para configurar o ambiente de desenvolvimento e instalar todas as bibliotecas necessárias para este projeto (Flask, Pillow e Azure AI Vision Image Analysis), execute o comando abaixo no seu terminal:

```bash
pip install flask azure-ai-textanalytics python-dotenv
